package controller.handlers.boardhandlers;

import javafx.event.Event;

public class ToggleGridHandler implements BoardHandler {
    @Override
    public void handle(Event event) {

    }
}
