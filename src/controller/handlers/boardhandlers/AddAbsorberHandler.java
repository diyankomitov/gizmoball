package controller.handlers.boardhandlers;

import model.board.BoardObjectType;

public class AddAbsorberHandler extends AddHandler {
    public AddAbsorberHandler() {
        super(BoardObjectType.ABSORBER);
    }
}
